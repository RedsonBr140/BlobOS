{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Launch QEMU",
            "type": "shell",
            "command": "qemu-system-x86_64 -cdrom build/BlobOS.iso -serial stdio -s -S -boot dc -d int"
        }
    ]
}